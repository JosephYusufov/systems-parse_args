#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

int main (){
  
  return 0;
}


char ** args = parse_args( line ){
  char foo[100] = "echo I am the Big Hancho"
  char *token = foo;
  char *cursor;
  token = strsep(&curr, " ");
  return;
}
